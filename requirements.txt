fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0

# Storage (R2, S3-compatible)
boto3

# DB access (directo a Postgres, local en dev / Supabase en prod) + Auth
sqlalchemy
alembic
pgvector
psycopg[binary]
pyjwt

# PDF extraction
pymupdf

# LLM providers vía LangChain — misma interfaz (BaseChatModel / Embeddings)
# para los dos, se elige uno en runtime vía LLM_PROVIDER
langchain-core
langchain-openai
langchain-google-genai
numpy

# Observabilidad — LangSmith traza automático al usar LangChain
langsmith

# Uploads multipart
python-multipart

# Donaciones (RF-07)
stripe

# Cola de background tasks con reintentos (chunking de CV) -- persistida en
# el mismo Postgres, sin Redis. Pineado porque su schema (db/alembic/versions/
# 8ec0f1c21f2a_add_procrastinate_schema.py) se lee del paquete instalado.
procrastinate==3.9.0

# Web Push (notificaciones) -- VAPID, sin proveedor externo ni cuenta de
# terceros de por medio.
pywebpush==2.4.0
